@{
    ViewBag.Title = "editroles";
}

@if (Request.IsAuthenticated)
{
    if (User.IsInRole("Admin"))
    {
    <p>Welcome to the Edit Roles page @User.Identity.Name</p>

    <p>ADMIN PAGE</p>
    <br />
    <br />
    <p>USERS</p>
    <br />
    <p>Test string: @ViewBag.teststring</p>
    <br />
        for (int i = 0; i < ViewBag.User.Count; i++)
        {
        <br />
        @ViewBag.user[i]
        <br />
        @ViewBag.userID[i]
        <br />
        @ViewBag.userROLE[i]
        <br />
           var messagelist = new List<String>();
            messagelist.Add(ViewBag.userID[i]);
            messagelist.Add("This is my message to you");
            messagelist.Add(ViewBag.userID[i]);

            ViewBag.messagesend = messagelist;
            

            @Html.ActionLink("Edit User Role", "testing", "Admin", new { message = Json.Encode(ViewBag.messagesend) }, null)
            <br />
        }
    <br />
    <br />
        if (ViewBag.message != null)
        {
            for (int i = 0; i < ViewBag.message.Count; i++)
            {
            <br />
            <br />
            @ViewBag.message[i]
            <br />
            <br />
            }
        }
    }
}